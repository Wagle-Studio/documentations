import { Alert } from '../../components/Alert'

# Hello MDX

Ceci est du **Markdown** rendu dans Next

- support GFM
- listes
- tableaux

| Col A | Col B |
| --- | --- |
| 1 | 2 |

<Alert type="info">
  Ceci est un composant React utilisé dans un fichier MDX
</Alert>

## Exemple de code

```ts
export const sum = (a: number, b: number) => a + b
```
